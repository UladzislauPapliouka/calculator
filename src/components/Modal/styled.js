import colors from '@constants/styles/colors';
import styled from 'styled-components';

const ModalWrapper = styled.div`
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: ${colors.gray43};
`;

export default ModalWrapper;
