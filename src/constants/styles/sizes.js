const borderWidths = {
  sm: 1,
  md: 2,
  lg: 3,
};

const borderRadiuses = {
  sm: 4,
  md: 8,
  lg: 12,
};

const fontSizes = {
  xs: 10,
  s: 20,
  xl: 32,
  xxxl: 54,
  xxxxl: 64,
};

const paddingSizes = {
  xxs: 4,
  xs: 10,
  s: 30,
  lg: 50,
};

const marginSizes = {
  xs: 10,
  s: 30,
  lg: 50,
};

const displayBreakpoints = {
  md: 768,
  lg: 1000,
  xxxl: 1400,
};

const underlineWidths = {
  sm: 2,
};

const gapSizes = {
  xs: 10,
  s: 20,
  l: 32,
  xxxl: 90,
};

const scrollbarWidth = {
  md: 10,
};

const keySizes = {
  sm: [40, 60],
  md: [50, 70],
  lg: [60, 80],
};

const rowSizes = {
  xs: 100,
  md: 130,
};

const columnSizes = {
  md: 400,
};
const headerSizes = {
  md: 70,
};

export {
  borderRadiuses,
  borderWidths,
  columnSizes,
  displayBreakpoints,
  fontSizes,
  gapSizes,
  headerSizes,
  keySizes,
  marginSizes,
  paddingSizes,
  rowSizes,
  scrollbarWidth,
  underlineWidths,
};
